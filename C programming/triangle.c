#include <stdio.h>
int main(void)
{
	float angle1, angle2, angle3, sum;
	
	printf("Enter angle 1 :");
	scanf("%f", &angle1);
	printf("Enter angle 2 :");
	scanf("%f", &angle2);
	printf("Enter angel 3 :");
	scanf("%f", &angle3);
	
	sum = angle1 + angle2 + angle3;
	
	if (sum = 180)
	{
	
	printf("Triangle can be formed");
	
    }
    else
	{
		printf("Not using given angle values");
	 } 
    
    return 0;
}
